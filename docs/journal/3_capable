## Technical Goal
For week 3 we are hoping to implement the score subsystem that will make the agent 
efficent and capable:
- combat
- movement
- goal management
I would hope that this would drive us to our final goal of beating in the minotuar

## Technical Uncertainty
- how do we handle polling for events, such as another player, npc or mob interacting with our
agents when its mid loop, to clarify, our input drives the agent, how do external events trigger our agent?
- there are key times where the agent need to dynamically updates it active plan eg. in comabt change stratagies, when traveling, change paths, how do we handle "check ins" with the agent.

## Technical Hypotheses 
I think we will solove combat and movement no problem.
I think the sticking point will be goal management, polling and dealing and finetuning the caps eg. max tokens and iterations. I think some kind of capable agent is possible I think we will end up leaving more serious planning on the table.

## Technical Observerations

## Step 1 - Moving Around
Trying to solve planning routes, exploring, and recalling memory

docs/plans/week_3/move_around.md

Claude noticed I have a week_2/plan_route.md which it said I should consider.
I determine that the amount of tool schema being added to the player was adding signifcantly to the token count. The options are reduce tools, but lose capabilities, or cache tool schema, but it can be done when the haiku token count is under 3K. I told it to explore delgating subsystems that handle tools calls.

## Step 2 - Detour with Tool Call Optimization

We need to optimize our tool calls this is detour to fix before we make our plan_route.

docs/plans/week_3/tool_call_optimization.md
We did alot of exploring in this document, and its clear that Claude Opus 5 can't make decisions
for us. Things we will do:
- create a facts table
- make sure we ahve class and gender information
- conditional tools based on class
- conditional tools based on location, situation
- reitroduce tools when need or force through a new composite tool.

I ended up going down a rabbit hole because if we introduce facts we need to conditionally inject them and also conditionally include tools, so ChatGPT and Claude started dicussing how we conditionally detect changes and we ended up look at building a planner for the goal and subgoal system. This is too much for now so lets keep on task.

## Step 3 House Cleaning
- comment out all tools we know we have yet to ever use. We will add them we nee them.
- Added profile.yaml params class and gender so we can get that into our knowledbase
docs/plans/week_2/add_missing_player_stats

## Step 4 - Moving Around Part 2

So I revisted it asking more questions and it gave me the option to have a planned route and the main agent does turn-per-turn, or the route is planned and once selected its executed, and needs a system to watch common events.

I want the latter obviously because its more effiecent and Claude wanted to use regex to detect common events to break out of that batched movement. But it surfaced an event where an NPC commented you not wearing anything. Tim pointed out talking to them gives a full suit of equiptment, the agent would have totally missed the oppurunity. 

It works very well.

⚙ plan_route(destination: "bakery")
[route] bakery — known
to: The Bakery (#12)
path: south → south → west → north
4 moves: The Temple Of Midgaard → The Temple Square → Market Square → Main Street → The Bakery

⚙ execute_route(steps: ["south","south","west","north"])
[route] executed 4/4
step 1: south → The Temple Square (ok)
step 2: south → Market Square (ok)
step 3: west → Main Street (ok)
step 4: north → The Bakery (ok)
arrived: The Bakery

I had to add back in `tbamud__shop` tool since the agent had no way to list the menu, and wasted considerable calls using examine.

One unexpected surpise was failing tools: 
⚙ examine(target: "baker")
error
ERROR: Boukensha::UnknownToolError: No tool registered as 'examine'
⚙ shop(action: "list", args: "")
error
ERROR: Boukensha::UnknownToolError: No tool registered as 'shop'

Haiku keeps dropping the prefix, we could amend the system prompt but this might keep failing even told to match the tool name.
THe fix for now: if a called name isn't registered but exactly one registered tool ends in __<name>, resolve to it.

docs/plans/week_3/toolname_resolve.md

I thought that the agent was running really fast but when I looked at Mud Monitor it would say 15 seconds.
I used Jaeger to get a breakdown and used my stop watch to track, I guess it is 15s but it just feels faster.

I was able to observe the fallback working
⚙ examine(target: "baker")
   A fat, nice looking baker.  But you can see that he has many scars on his
body.
The baker is in excellent condition.

The baker is using:
<wielded> 

Though the agent keeps thinking it needs to examine first the bakery instead of using the shop.
CircleMud supports more than one shop in a room 
list baker
list armorer
We need to figure a way to force the correct command.

## Step 5 - Correct Context For Commands

Context Commands is about ensuring the correct commands are getting called based on if the agent is in a room, it should have affordance to know that it can buy, sell, and etc. or if in a bank to use the bank commands. 

docs/plans/week_3/context_commands

I developed this plan with Claude but I realize I really need to write out agent scenarios for us to test against.

## Step 6 - Agent Scenarios

Agent scenarios will act like our test suite. 
We define a start state of the player, we execute said task via a sesssion, the AI will anaylze and determine how it performed against what we expect the agent to do.

I have created a docs/AGENT_SCENARIOS.md

I think I need to add the ability to name a session, so we can programmtically trigger a session run.

docs/plans/week_3/batch_sesssion_tesing.md

We can now run batches of scenarios, currently my find_bakery scenario test is failing because it loads no map as intended when seeding the user. Random movement we overlooked earlier we know it does random fanout movement. 

We need create better logic for random exploration, I think the agent having a sense of boundaries will help with navigation

## Step 7 Boundary Awarneess

When attempting to plan with Claude is tried use regex per usual to identify places which I told is brittle. Then it came up with a flag system where it would plant flags and add descriptions, but it seemed like exceesive flags being placed. So I told it should just create a region, add places as it goes, and decide to split a region when it grows to large.  

I think the hard part is the fact that as a region grows more frontiers appear, and going to the closest frontier doesn't make sense you need to reason which frontier to take.

docs/plans/week_2/boundaries.md

I had to rewrite the tech doc.
I was getting tired of the prosed being used to write tech doc so I created a skill called tech-prose. its not in this repo.

docs/plans/week_2/boundaries_revised.md

I had it execute the plan and it created a plan for boundaries_gate.
A few problems:
- agent never called its new region commands to create a region or split a region
- in cases where it could plan a route it simply never executed execute_route.
Its very hard to get this agent to call the tools we want it to call.

## Step 8 Robust Snapshots and Prior Knowledge Sesssions

The problem with running sessions is that it keeps clearning the current knowledgebase
and so I have no reference to the old ones. These sqlite databases are quite small so it seems like we should store snapshots folder and a previous sessions knowledge up to so many before deleteing.

docs/plans/week_3/session_memory.md

### Step 6 - The Planner 

I think I have been spinning my wheels around the same problem.
And I think I might need to implement this now. The big problem is that
by adding the planne we are opening the biggest can of worms.

Aside, our main problem is `exmaine` and `move` becacause they are so generic
that they can fired off when it doesn't make sense.

Instead having `move` we could have `explore` which underneath would have to handle
logic like region mapping.






## Technical Conclusions
Reflecting back your education guesses from the technical uncertainty section what was the technical outcomes. Is there any new technical uncertainty that has been put aside for future exploration. Are there any next steps or technical considerations worth noting?


## Key Takeaway
In one sentence. State the most important lesson from the week.
